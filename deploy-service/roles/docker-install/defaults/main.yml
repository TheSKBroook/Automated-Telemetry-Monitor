---
# defaults file for docker-install
docker_apt_dependencies:
  - ca-certificates
  - curl
  - gnupg
docker_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
